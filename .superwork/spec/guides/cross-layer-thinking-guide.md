# Cross-Layer Contracts

- Trace changes across `apps/server/src`, `packages/api/src`, `packages/sync/src`, and `packages/swift` when a client/server message changes.
- Update producers and consumers, including `apps/ios` and `apps/macos` when the native UI observes the contract.
- Cover serialization, validation, and error behavior in the affected package's tests and native tests where applicable.
