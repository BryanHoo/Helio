# Frontend Placement

- Keep web routes and page components in their owning app; shared API types belong in `packages/api`.
- Keep macOS/iOS-only views in their app and shared UI in an existing `packages/swift` module.
- Check both native clients when changing shared Swift presentation contracts.
