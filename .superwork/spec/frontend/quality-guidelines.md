# Frontend Verification

- Run `bun run --cwd apps/www typecheck` and `bun run --cwd apps/www build` for website changes.
- Run `bun run --cwd apps/cloud test` for cloud page behavior.
- For native UI, follow `.agents/skills/tophat/SKILL.md` and run affected Swift tests and builds.
